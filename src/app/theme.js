import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    transparent: "transparent",
    black: "#000",
    white: "#fff",
    blue: {
      50: "#89b4fa",
      100: "#74c7ec",
      150: "#89dceb",
      200: "#1e66f5",
    },
  },
});
